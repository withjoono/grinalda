import 'package:flutter/material.dart';

class HeatmapStatisticsScreen extends StatelessWidget {
  const HeatmapStatisticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("잔디밭 통계"),
    );
  }
}
