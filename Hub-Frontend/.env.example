# ================================
# 거북스쿨 프론트엔드 환경 변수 설정
# ================================
# 이 파일을 복사하여 .env.local 파일을 생성하세요
# cp .env.example .env.local
# ⚠️ 중요: .env.local 파일은 절대 Git에 커밋하지 마세요!

# ================================
# 프론트엔드 URL
# ================================
# 현재 프론트엔드 애플리케이션의 URL
# 개발: http://localhost:3000
# 프로덕션: https://geobukschool.kr
VITE_FRONT_URL="http://localhost:3000"

# ================================
# 백엔드 API URL
# ================================
# Hub 중앙 인증 서버 (NestJS): 로그인, 회원가입, OAuth, SSO
# ⚠️ 필수: 인증 기능에 사용됨
VITE_API_URL_HUB="http://localhost:4000"

# NestJS 백엔드 (로컬 개발용 프록시 대상)
VITE_API_URL_NEST="http://localhost:4000"

# Susi 비즈니스 로직 백엔드: 수시/정시 전략, 학생부 관리
VITE_API_URL_SUSI="http://localhost:4001"

# Spring 백엔드는 더 이상 사용하지 않음 (2024-12 NestJS로 완전 마이그레이션)
# VITE_API_URL_SPRING="http://localhost:8080"

# ================================
# SSO (Single Sign-On) 설정
# ================================
# Hub OAuth 클라이언트 ID
# ⚠️ 필수: SSO 인증 플로우에 사용됨
VITE_HUB_OAUTH_CLIENT_ID="geobukschool-frontend"

# Hub 메인 페이지 URL (SSO 중앙 인증 서버)
# SSO 로그인 시 이 URL로 리다이렉트됨
VITE_HUB_URL="http://localhost:3000"

# SSO 연동 서비스 URL
# 각 서비스는 Hub에서 발급받은 토큰으로 인증됨
VITE_SUSI_URL="http://localhost:3001"
VITE_JUNGSI_URL="http://localhost:3002"
VITE_MYEXAM_URL="http://localhost:3003"
VITE_STUDYPLANNER_URL="http://localhost:3004"

# 레거시 (호환성)
VITE_EXAMHUB_URL="http://localhost:3003"

# ================================
# 소셜 로그인 설정
# ================================
# 네이버 로그인 클라이언트 ID
# 발급: https://developers.naver.com/apps
# ⚠️ 주의: 네이버 개발자 센터에서 Callback URL 등록 필수
VITE_NAVER_LOGIN_CLIENT_ID="your-naver-client-id"

# 구글 로그인 클라이언트 ID
# 발급: https://console.cloud.google.com/apis/credentials
# ⚠️ 주의: Firebase와 연동된 OAuth 클라이언트 ID 사용
VITE_GOOGLE_CLIENT_ID="your-google-client-id"

# ================================
# Firebase 설정
# ================================
# Firebase Console에서 프로젝트 설정 > 일반 > 앱 추가 > 웹
# 발급: https://console.firebase.google.com
# ⚠️ 주의: 구글 소셜 로그인에 필수
VITE_FIREBASE_API_KEY="your-firebase-api-key"
VITE_FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="your-project-id"
VITE_FIREBASE_STORAGE_BUCKET="your-project.firebasestorage.app"
VITE_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
VITE_FIREBASE_APP_ID="your-app-id"
VITE_FIREBASE_MEASUREMENT_ID="your-measurement-id"

# ================================
# 결제 서비스 (Iamport)
# ================================
# 아임포트 가맹점 ID (https://admin.iamport.kr)
VITE_IMP_MERCHANT_ID="your-iamport-merchant-id"

# ================================
# GCP Cloud Storage (선택사항)
# ================================
# VITE_GCP_STORAGE_BUCKET="your-gcs-bucket"
# VITE_GCP_CDN_URL="https://cdn.your-domain.com"

# ================================
# SSO 보안 체크리스트
# ================================
# ✅ VITE_HUB_OAUTH_CLIENT_ID가 백엔드 oauth_clients 테이블에 등록됨
# ✅ 각 SSO 서비스 URL이 백엔드의 허용된 redirect_uri 목록에 포함됨
# ✅ Firebase 프로젝트에 구글 로그인 제공자가 활성화됨
# ✅ 네이버 개발자 센터에 Callback URL 등록됨
# ✅ 프로덕션 환경에서 HTTPS URL 사용
