name: Deploy Backend to Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - 'Hub-Backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

env:
  PROJECT_ID: ts-back-nest-479305
  REGION: asia-northeast3
  SERVICE_NAME: geobukschool-backend
  CLOUD_SQL_INSTANCE: ts-back-nest-479305:asia-northeast3:geobuk-db

jobs:
  deploy:
    name: Deploy to Cloud Run
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: |
          gcloud auth configure-docker gcr.io

      - name: Build Docker image
        working-directory: ./Hub-Backend
        run: |
          docker build -t gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
                       -t gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:latest .

      - name: Push Docker image to GCR
        run: |
          docker push gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }}
          docker push gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --memory 1Gi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 10 \
            --port 8080 \
            --timeout 300 \
            --add-cloudsql-instances ${{ env.CLOUD_SQL_INSTANCE }} \
            --set-env-vars="NODE_ENV=production,AUTH_JWT_TOKEN_EXPIRES_IN=7200000,AUTH_REFRESH_TOKEN_EXPIRES_IN=5184000000,IMP_KEY=test_key,IMP_SECRET=test_secret,IMP_STORE_CODE=test_store,ALIGO_TEST_MODE=true,GCS_PROJECT_ID=ts-back-nest-479305,GCS_BUCKET_NAME=ts-back-nest-bk,GCS_PUBLIC_URL=https://storage.googleapis.com/ts-back-nest-bk" \
            --update-secrets="DATABASE_URL=database-url:latest,AUTH_JWT_SECRET=jwt-secret:latest,AUTH_REFRESH_SECRET=auth-secret:latest,FIREBASE_SERVICE_ACCOUNT=firebase-service-account:latest"

      - name: Show service URL
        run: |
          gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region ${{ env.REGION }} \
            --format 'value(status.url)'
