# Hub 프로젝트 개발 가이드

## 프로젝트 개요

**Hub**는 대학 입시 컨설팅 서비스를 위한 풀스택 웹 애플리케이션입니다.

### 기술 스택

- **프론트엔드**: React 18 + TypeScript + Vite + TanStack Router
- **백엔드**: NestJS + TypeORM + PostgreSQL
- **인프라**: Docker (PostgreSQL, Redis)

---

## 🚀 빠른 시작

### 1. 사전 준비

- **Node.js**: v20 이상
- **Docker Desktop**: 실행 중이어야 함
- **Yarn**: 백엔드 패키지 관리자

### 2. 서버 실행

```bash
# Windows
./start-dev.bat

# 또는 수동 실행
docker start geobuk-postgres geobuk-redis
cd Hub-Backend && yarn start:dev
cd Hub-Frontend && npm run dev
```

### 3. 서버 종료

```bash
# Windows
./stop-dev.bat

# 또는 수동 종료
# Ctrl + C로 각 터미널 종료
# docker stop geobuk-postgres geobuk-redis
```

---

## 📦 포트 설정 (필수)

| 서비스 | 포트 | 변경 금지 |
|--------|------|----------|
| **프론트엔드** | **3000** | ✅ 고정 |
| **백엔드** | **4000** | ✅ 고정 |
| **PostgreSQL** | **5432** | ✅ 고정 |
| **Redis** | **6379** | ✅ 고정 |

### 포트 설정 파일

1. **프론트엔드**: `Hub-Frontend/package.json`
   ```json
   "dev": "vite --port 3000 --host"
   ```

2. **백엔드**: `Hub-Backend/.env.development`
   ```env
   PORT=4000
   DB_PORT=5432
   REDIS_PORT=6379
   ```

---

## 🔧 개선된 기능

### start-dev.bat 자동화

✅ **포트 충돌 자동 해결**
- 포트 3000, 4000 사용 중인 프로세스 자동 종료
- 수동 개입 불필요

✅ **환경 변수 자동 생성**
- `.env.development` 파일 자동 생성
- 필수 환경 변수 자동 설정 (더미 값)

✅ **Docker 컨테이너 자동 시작**
- PostgreSQL, Redis 컨테이너 자동 실행
- 없으면 자동 생성

---

## 🐛 문제 해결

### 포트 충돌 발생 시

```bash
# 포트 사용 확인
netstat -ano | findstr "3000"
netstat -ano | findstr "4000"

# 프로세스 강제 종료
taskkill /F /PID [PID번호]

# 또는 stop-dev.bat 실행
./stop-dev.bat
```

### Docker 컨테이너 재생성

```bash
# 기존 컨테이너 삭제
docker stop geobuk-postgres geobuk-redis
docker rm geobuk-postgres geobuk-redis

# start-dev.bat 실행하면 자동 재생성
./start-dev.bat
```

### 환경 변수 오류

```bash
# .env.development 파일 삭제
rm Hub-Backend/.env.development

# start-dev.bat 실행하면 자동 재생성
./start-dev.bat
```

---

## 📁 프로젝트 구조

```
Hub/
├── Hub-Frontend/          # React 프론트엔드 (포트 3000)
│   ├── src/
│   ├── package.json      # "dev": "vite --port 3000 --host"
│   └── ...
├── Hub-Backend/           # NestJS 백엔드 (포트 4000)
│   ├── src/
│   ├── .env.development  # PORT=4000 (자동 생성)
│   └── ...
├── start-dev.bat         # 통합 시작 스크립트
├── stop-dev.bat          # 통합 종료 스크립트
└── HUB-DEVELOPMENT-GUIDE.md  # 이 문서
```

---

## 🌐 접속 URL

개발 서버 실행 후:

- **프론트엔드**: http://localhost:3000
- **백엔드 API**: http://localhost:4000
- **Swagger 문서**: http://localhost:4000/swagger
- **PostgreSQL**: localhost:5432
- **Redis**: localhost:6379

---

## ⚡ 성능 개선

### 이전 문제점

1. ❌ 포트 충돌 시 수동 종료 필요
2. ❌ 환경 변수 누락 시 수동 생성 필요
3. ❌ 여러 번의 재시작 시도

### 개선 후

1. ✅ 포트 충돌 자동 해결 (2초 이내)
2. ✅ 환경 변수 자동 생성
3. ✅ 한 번의 명령으로 전체 서버 실행
4. ✅ 예상 시작 시간: **10-15초**

---

## 🔒 보안 참고사항

### 개발 환경 (더미 값)

`.env.development` 파일의 값은 개발용 더미 값입니다:
- ✅ 로컬 개발에만 사용
- ✅ Git에 커밋되지 않음 (.gitignore)
- ⚠️ 프로덕션에서 절대 사용 금지

### 프로덕션 환경

실제 서비스 배포 시 다음 값들을 반드시 교체하세요:
- `AUTH_JWT_SECRET`
- `AUTH_REFRESH_SECRET`
- `ENCRYPTION_KEY`
- `IMP_KEY`, `IMP_SECRET` (결제 서비스)
- `DB_PASSWORD` (강력한 비밀번호)

---

## 📞 지원

문제 발생 시:

1. `./stop-dev.bat` 실행하여 모든 서버 종료
2. Docker Desktop 재시작
3. `./start-dev.bat` 재실행
4. 여전히 문제 발생 시 이슈 등록

---

## 📝 변경 이력

### 2026-01-20
- ✅ 포트 충돌 자동 해결 기능 추가
- ✅ 환경 변수 자동 생성 기능 추가
- ✅ start-dev.bat 스크립트 개선
- ✅ 개발 가이드 문서 작성

---

**Happy Coding! 🎉**
